apiVersion: v1
kind: Namespace
metadata:
  name: elvis-namespace
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment
  namespace: elvis-namespace
  labels:
    app: flask
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      
      containers:
      - name: flask
        image: elvis/flaskapp
        ports:
        - containerPort: 5000
        imagePullPolicy: Always
        

---